import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.activitytracker.mobile',
  appName: 'activitytracker',
  webDir: 'dist'
};

export default config;
